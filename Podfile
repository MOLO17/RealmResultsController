# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RealmResultsController' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RealmResultsController
  pod 'RealmSwift'

  target 'RealmResultsController-iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'RealmResultsController-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RealmResultsController-iOS

end
